from ... import ...
from random import randint
def gen_prost(N):
    x = randint(N, 2*N)
    while not prime(p):
        if not prime(x):
            p += 1
        elif p == 2*N:
            p = N
   return p
